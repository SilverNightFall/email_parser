<%= form_for @attachment, :html => {:multipart => true} do |f| %>
<% if @attachment.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>
  <ul>
    <% @attachment.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <%= f.file_field(:email, :class => 'btn btn-primary btn-small') %>
  <div>
<%= f.submit("Save", {:class => "btn btn-primary btn-small" }) %> <%= submit_tag("Reset", :name => 'reset', :id => 'reset_button', :type => "reset", class: "btn btn-primary btn-small") %>
</div>
<% end %>
